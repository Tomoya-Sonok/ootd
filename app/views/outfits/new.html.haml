.main
  .main__upper
    .container
      = link_to outfits_path, class: 'backward col-md-1' do
        .back-icon
          = icon('fa fa-2x','angle-double-left')
        .backward-btn
          戻る
      .main__upper__title
        コーデの追加
      .main__upper__cover
        = form_for @outfit do |f|
          .row
            #img_field{:onclick => "$('#file').click()"}

              %i.fas.fa-image
              %i.fas.fa-file-upload.add
            = f.file_field :image, class: "image", style: "display:none;", id: "file"
            .col-md
              .form-group#flexbox
                #add-message
                  右のボタンを押すとリセット
                  %label#add-outfit
                    = link_to icon('fa fa-2x','undo-alt'), new_outfit_path

          .row
            .col-md
              .form-group
                = f.label "コーデ名", id: 'outfit-name' 
                = f.text_field :name, class: 'form_control'
          -# .row
          -#   .col-md
          -#     .form-group
          -#       = fields_for @outfit_tag do |t|
          -#         = t.label "カテゴリー", id: 'outfit-tag'
          -#         = t.select :tagname, options_for_select(Tag.all.pluck(:tagname).uniq.map.with_index(1){|o, index| [o, {'data-tag_id'=>index}]}, Tag.first.tagname), {}, {class: 'form-control', id: 'category-form'}
          -#         -# = t.collection_select( :id, Tag.all.pluck(:tagname).uniq.map.with_index(1){|o, index| [o, {'data-tag_id'=>index}]}, :id, Tag.first.tagname)
          -#         -# 上のselectの記述でtag_idを取得できれば、あとはコントローラに必要な一文書くだけ！
          .row
            .col-md
              .form-group
                = f.label "", id: 'add-btn' do
                  = f.submit '追加する', class: 'btn btn-success'
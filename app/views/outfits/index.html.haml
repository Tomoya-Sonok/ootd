
#modal.modal-content
.main
  .main__upper
    .main__upper__actions
      .main__upper__actions__category
        = form_for @outfit_tag, url: outfits_path do |form|
          .form-group
            = form.label "カテゴリー選択"
            = form.select :tagname, options_for_select(Tag.all.pluck(:tagname).uniq.map.with_index(1){|o, index| [o, {'data-tag_id'=>index}]}, Tag.first.tagname), {}, {class: 'form-control', id: 'category-form'}
            .tag-add-group
              = form.text_field :tagname, class: 'add-tag-form'
              = form.submit '追加', class: 'tag-add btn btn-success'
      
      .modal-open.button-2
        今日のコーデを決める！
  .main__lower
    .container
      .main__lower__title
        コーデ一覧
        = link_to new_outfit_path, class: "add-path" do
          %span コーデを追加
          %i.fas.fa-arrow-right
      #outfit-list
        - @outfits.each do |outfit|
          = link_to outfit_path(outfit) do
            .col-md-4#a-outfit
              = image_tag outfit.image.to_s, id: "outfit-image"
              #outfit-tag
                - outfit.tags.each do |tag|
                  = tag.tagname
              #outfit-name
                = outfit.name
      #selected-list
-# jQueryで、カテゴリーが選択された時には、そのカテゴリーのコーデリスト
-# だけを表示できるようにする。その際は、上記の#outfit-listはdisabledにする
